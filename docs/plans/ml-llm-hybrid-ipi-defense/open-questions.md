# Open Questions

Tracked decisions or clarifications needed across aigate plans.

## opensrc-integration-plan - 2026-04-20

- [x] opensrc `sources.json` live schema — Resolved via coordinator probe `.omc/plans/opensrc-probe.md` (2026-04-20).
- [x] `registry` enum for aigate-specific ecosystems → **Emit as-is** lowercase ecosystem name (`pub`, `cocoapods`, `crates`, `pypi`). opensrc's enum may extend; aigate-provenance sibling file captures our-specific metadata. Deterministic + discoverable. (Coordinator decision 2026-04-20, autonomous authority.)
- [x] CocoaPods without `GITHUB_TOKEN` → **Degrade to metadata-only** scan (warn, don't hard-fail). Podspec JSON still analyzable; GitHub rate-limit is recoverable; hard-fail would break most users' first-run experience. (2026-04-20)
- [x] AGENTS.md conflict policy → **Append with idempotent marker** matching existing CLAUDE.md/GEMINI.md pattern. Non-destructive, re-runnable. (2026-04-20)
- [x] Phase fallback on acceptance-gate failure → **Halt and surface**: commit completed phases, stop autonomous run, report failing phase + logs. Cascading skips hide debt; explicit halt is safer for autonomous mode. (2026-04-20)
- [x] Ecosystem ordering (crates vs cocoapods) → **Keep current order** (Rust Phase 2, CocoaPods Phase 3). Rust is simpler/lower-risk first; CocoaPods has GITHUB_TOKEN wrinkle benefiting from Rust precedent. (2026-04-20)
- [x] Concurrent scan + opensrc emit performance → v1: accept flock serialization. **v2 REVISION (Architect P2 #4, 2026-04-20)**: use atomic-replace idiom matching `cache.py:76-83` (tempfile + `os.replace`) with optimistic-concurrency re-read + 3-retry, run inside `asyncio.to_thread`. No flock. Crash-safe, non-blocking.

## opensrc-integration-plan v2 — 2026-04-20 (post-Architect review)

- [x] Upstream contract with opensrc → **Ship v2 as-is; file follow-up issue in Phase 5** (post-merge). Proposed-writer contract (schema_version, reserved extension fields, registry-enum leniency) is a protocol-level concern that warrants OSS discussion, not a launch blocker. Phase 1 gate stays. (2026-04-20)
- [x] `--opensrc-overwrite` default → **`never`** (safe default; preserves opensrc user's existing cache). Power users opt-in via `--opensrc-overwrite=when-aigate-wins` or `.aigate.yml`. No silent destruction. (2026-04-20)
- [x] `source_unavailable` downgrade scope → **Extend cross-ecosystem**. If npm 404 / pypi timeout / crates 503 makes tarball unreachable, set `NEEDS_HUMAN_REVIEW` (exit 1), never SAFE. Principle enforcement: never claim SAFE on uninspected bytes. CocoaPods rate-limit is just one instance of this general rule. (2026-04-20)
- [x] CocoaPods without GITHUB_TOKEN (v1 RESOLVED → v2 REVISED per Architect P1 #3): degrade must set `NEEDS_HUMAN_REVIEW` (exit 1), NOT SAFE (exit 0). Never claim SAFE on uninspected bytes. (2026-04-20)
- [x] Registry enum for JSR (v1 ambiguous → v2 RESOLVED): emit `"jsr"` as-is, not `"npm"`. Matches coordinator "emit as-is" decision; v1 PRD table had a JSR→npm mapping that contradicted it. Fixed in v2. (2026-04-20)

## aigate-phase1b-birdcage-backend - 2026-04-21

- [ ] **Birdcage machine-readable output surface** (Task 0 BLOCKING) — Does upstream Birdcage emit a stable JSON-lines stream, or will we need a side-channel log file / narrowed fs-only surface via fanotify? Must resolve before Task 2 lands. Gates the entire parser design.
- [ ] **`BIRDCAGE_MIN_VERSION` pin** — Which upstream Birdcage version becomes the floor? Must be pinned in Task 2 PR. Too new → user friction ("cargo install birdcage" failing on stable toolchains); too old → unstable event schema.
- [ ] **macOS `sandbox-exec` profile text** — Do we ship the SBPL profile inline in `birdcage_backend.py` or let upstream Birdcage own it? Leaning inline (auditability), but TBD pending Task 0.
- [ ] **Network policy in 1b** — PRD says default `deny-outbound`, but Phase 1b cannot implement mitmproxy. Proposed: 1b forces `network_policy=allow` regardless of config, with a WARN that network-capture is Phase 2. Needs user sign-off.
- [ ] **npm lifecycle-script toggle** — `--ignore-scripts=false` is required for realistic observation but makes the sandbox run arbitrary preinstall code. The sandbox is literally designed for this, but document the trust model in runbook.
- [ ] **Resource accounting on macOS** — `resource.getrusage(RUSAGE_CHILDREN)` works on macOS but `max_rss` units differ (KB on Linux, bytes on macOS). Need platform-normalized `max_rss_mb` or skip the field on macOS.
- [ ] **CI runner with Birdcage** — Do we add a nightly CI job with birdcage installed, or keep integration tests opt-in / locally-run for Phase 1b and land CI in Phase 2?

## aigate-sandbox-mode-prd — 2026-04-20

- [x] GPL-3.0 Birdcage subprocess → **Acceptable**. Subprocess boundary = no viral contamination; aigate stays Apache-2.0. Offer `--sandbox-mode=docker-only` escape hatch for users with stricter licensing. (2026-04-20 coordinator)
- [x] Docker image registry + cosign → **GHCR under `ImL1s/aigate-sandbox`** with GitHub Actions OIDC keyless cosign (no manual key custody). Image tagged by aigate version + SHA digest pinning. (2026-04-20)
- [x] CI default `sandbox.required` → **`false`** even in CI. Surface WARN if sandbox requested but runtime absent; only hard-fail when user explicitly sets `sandbox.required=true`. Too strict a default would break first-run UX for users who don't understand the feature yet. (2026-04-20)
- [x] Benchmark SLA → **≥90% detection / ≤2% FP on DataDog 22k dataset is the ship gate for Phase 6**. Not required to beat GuardDog — they're static-rule complementary, not competitor. Track comparative delta in benchmark writeup but don't gate on parity. (2026-04-20)
- [x] Phase 5 ecosystem ordering → **Confirmed: pypi → crates → jsr → pub → cocoapods**. Rationale: pypi highest install volume; crates novel build.rs surface; jsr reuses npm path (cheap); pub/cocoapods are mobile niche (pub simpler than cocoapods git-sources). (2026-04-20)

## aigate-sandbox-mode-prd v2 — 2026-04-20 (post-Architect iteration)

- [x] **Hakoniwa stub timing** → **Land in Phase 2** (0.5 day insurance vs scramble cost). Tightens the `SandboxBackend` ABC under two implementations early; Birdcage license change is a real future risk worth pre-paying for. (2026-04-20 coordinator)
- [x] **CI starter-workflow template scope** → **YES, includes `docs/examples/github-actions.yml`**. NON-goal #11 (static-only reference metric) MUST cover all aigate-published CI templates, not just marketing prose. Templates ship with `--sandbox` OFF + commented-out opt-in block. Consistency = guard-rail durability. (2026-04-20)
- [x] **Phase-1 cache-ordering validation** → **YES, mandatory code-read with line-number citation as Phase 1 acceptance criterion**. Add to Phase 1 acceptance: "Confirm prefilter ordering at cli.py:_check (cite exact lines); if not pre-sandbox, file as Phase-2 reorder task and document fail-closed parallelism." Don't skip the check. (2026-04-20)

## aigate-sandbox-mode-prd v3 — 2026-04-20 (post 5-agent multi-perspective critique)

- [x] **Canary scheme fingerprint lifetime** → **Fresh-per-run default**; stable-across-runs mode NOT shipped. Trace-diffing uses canonical-form hash, not literal token values. Stable canary gives attacker learnable constant = defeats the mechanism. (2026-04-20 coordinator)
- [x] **Replay-artifact license** → **Publishable** on HuggingFace/S3 with Apache-2.0 attribution in NOTICE file. Traces are derivative works compatible with permissive license. Defer actual publication to Phase 7 (stretch); Phase 6a uses private self-hosted captures. Legal confirm before community publish. (2026-04-20)
- [x] **CI cost calculator** → **Deferred to Phase 2+**. `aigate doctor --sandbox --estimate-ci-cost` is great UX but Phase 1 priority is correctness (P0 fixes). Ship Phase 1 without calculator; add as Phase-2 DX improvement. (2026-04-20)
- [x] **`paranoid` deprecation window** → **Two releases** with `DeprecationWarning`. Enterprise version-pinning is real; aggressive removal breaks CI. (2026-04-20)
- [x] **Decoy bind-mount scheme transparency** → **Public documentation**. Tokens are per-run random; scheme disclosure doesn't leak values. Transparency principle beats security-through-obscurity. Document in `docs/sandbox.md`. (2026-04-20)
- [x] **Canary mount visibility (R21)** → **Defer to Phase 4**. `/proc/mounts` over-mount is kernel-level complexity; scope creep for Phase 1. Document as known Phase-1 limitation; sophisticated attackers can detect bind-mounts. Mitigated by: canary reads are still captured before detection is useful. (2026-04-20)
- [x] **`SECRET_ENV_PATTERNS` self-update** → **Hand-maintained default**; optional integration with `detect-secrets` / `gitleaks` rule sets deferred to Phase 5+. Auto-sync introduces supply-chain risk to aigate itself. Manual review of pattern additions is safer. (2026-04-20)
- [x] **Phase 6a capture budget** → **Self-hosted runner** preferred for cost + control + consistency. Community contribution model as Phase 7 stretch (trace-contrib-via-PR workflow). (2026-04-20)
- [x] **Telemetry opt-in** → **Defer to post-MVP**. No telemetry in v1 ship. Revisit after 6 months of user feedback; must be GDPR-compliant + opt-in + clear value exchange. (2026-04-20)

## aigate-sandbox-mode-prd v3.1 — 2026-04-20 (post Architect v3 review)

- [x] **CocoaPods `:git` Phase 5 handling** → **Metadata-only downgrade, consistent with P0-3 "zero outbound"**. Don't selectively violate the invariant for one ecosystem — principle integrity matters more than one-ecosystem coverage. Phase 5 documents this as known CocoaPods limitation; users can use opensrc cache if they need deeper analysis. (2026-04-20 coordinator)
- [x] **Phase 1 doctor/init effort** → **Accept Architect's 7-10 day calibration**. Don't trim checks — doctor is the primary rage-quit mitigation (P1-9). Better to ship fewer phases with solid doctor than rush Phase 1. (2026-04-20)
- [x] **Pre-release replay-validity enforcement** → **GHA gate blocking release-tag push**. Manual checklist degrades; E3's whole point is silent-decay prevention. GHA friction acceptable (adds ~30s to release workflow). (2026-04-20)
- [x] **`__cache_schema_version__` bump cadence** → **Bump on any breaking format change**: canary scheme struct change, observer primitive added/removed, secret-patterns major version change. Document in CHANGELOG under dedicated `Cache Invalidation` section. Patch releases NEVER bump (docs/resolver tweaks are cache-safe). (2026-04-20)
- [x] **Redaction scrub-set perf budget** → **Measure in Phase 1 benchmark**; if >10% of total sandbox latency, optimize to Aho-Corasick. Not a blocker for v3.1 ship; latency budget has 20% headroom vs R15's high end. (2026-04-20)
- [x] **macOS gap docs placement** → **All three** (README + `docs/sandbox.md` + `aigate doctor --sandbox` output). Triple-surface for discoverability; users must not silently believe macOS-light == Linux-light. (2026-04-20)
- [x] **Analyst v3.1 re-review** → **Skip**. v3.1 delta is localized to Architect v3's E1-E5 identified issues; running another Architect pass is diminishing returns. Critic can directly verify E1-E5 resolution. If Critic surfaces orthogonal gap, revisit. (2026-04-20)

## aigate-phase2-observation-layer — 2026-04-21

- [ ] **`--sandbox-observer` CLI flag** — Do we expose a force-specific-observer flag (e.g. `--sandbox-observer=strace`), or is auto-detect via `runtime_select.detect_linux_connect_observer()` always sufficient? Flag adds surface but helps debuggability. Proposed: defer to Phase 3 unless reviewer insists.
- [ ] **`PARSER_PARTIAL_DRIFT` on observer stream** — Phase 1b already wires drift-on-birdcage-stream into `policy.decision_from_dynamic_trace()`. Should drift-on-observer-stream reuse the same NEEDS_REVIEW escalation, or have a distinct severity? Leaning reuse (Principle 3 — parser never SAFE on drift).
- [ ] **macOS coverage-matrix wording** — "network deny by kernel, no observation" vs "observed-via-kernel-deny" vs "kernel-denied, unobserved". Semantic framing affects user mental model. Needs docs decision before Task 2.8.
- [ ] **Stuck-observer timeout budget** — 10s proposed; lodash installs finish in ~3s so generous, but monorepo installs may have natural quiet windows (tarball download phase). May need per-ecosystem tuning or bump to 15s. Reviewer input requested.
- [ ] **`ptrace_scope=2` handling** — Fail-closed with actionable error is planned. But should `aigate doctor --sandbox` offer the `sysctl kernel.yama.ptrace_scope=1` fix, or just surface the state? Leaning surface-only (aigate never sudo).
- [ ] **E2E runner CI integration** — Phase 1b deferred CI with birdcage to Phase 2. With strace added, CI fleet needs birdcage + strace + npm. Ubuntu GitHub-hosted runner viable; self-hosted alternative? Blocks Task 2.7 CI gating.

## aigate-phase3-evasion-detectors - 2026-04-21

- [ ] **`SBPL_EXPECTED_SKIPS` set contents** (T6 / D5) — Beyond `DIRECT_XPC`, should `CANARY_ABSOLUTE_PATH_WRITES` also move to the macOS expected-skip set? Currently listed under `SandboxCoverage` enum but Phase 2 canary observer partially covers it. Resolve before T6 merge so Architect can rule on Phase-4 deferred scope.
- [ ] **Entropy threshold 7.2** (T5 `derived_exfil`) — Is 7.2 Shannon entropy the right cut-off, or should it be platform-tuned (macOS writes tend higher due to HFS+ compression)? Needs a calibration pass against the `tests/fixtures/` corpus before we ship.
- [ ] **Multi-evasion aggregator threshold = 5** (T8, D4) — Is 5 the right number, or should it scale with package size (e.g. 5 for small, 10 for monorepos)? Start fixed, revisit if FP reports arrive.
- [ ] **Fixture provenance policy** (R6) — Do we check fixtures in as-is (vendored snippets from public IOC reports) or transform them (comment the origin URL + sanitize obvious identifiers)? Leans sanitized+attributed; needs Architect sign-off on the licensing footprint.
- [ ] **`build_hooks` dynamic correlation window = 500ms** (T4) — Arbitrary; need to measure on the `fake_npm_net_connect_bypass` fixture and adjust. Part of the T4 acceptance deliverable.
- [ ] **`anti_debug` dev-dependency exclusion path list** (R8) — Listed `node_modules/.bin/` + `.github/workflows/` as excluded; complete list needs review once we survey popular debugger deps (pdb, node --inspect, ipdb).
- [ ] **Doctor JSON shape for `evasion.*` counters** (T12) — New fields `detectors_loaded`, `detectors_fired_last_run`, `signals_collapsed`. Should these live under the existing sandbox section or a new top-level `evasion` key? Prefer nested under `sandbox.evasion` for discoverability; Architect pick.

## ml-llm-hybrid-ipi-defense - 2026-04-22

- [ ] **`short_circuit_both` shipping strategy** — Should ML-only MALICIOUS short-circuit (Stage-3) be implemented as dead code behind a flag in this plan, or deferred entirely to a future plan once Stage-2 evidence exists? Leaning defer — reduces PR-5 LOC and test surface, prevents accidental early-enable.
- [ ] **Prompt-Guard-2 default variant** — Default `ipi_defense.classifier.model` to Llama-Prompt-Guard-2-22M (smaller, English-only, CPU-friendly) vs `-86m` (multilingual)? aigate reads code (mostly English), so 22M likely sufficient. Why it matters: impacts wheel-size budget for users who opt-in to `[classifier]` extra.
- [ ] **Telemetry endpoint ownership** — Host ourselves at `aigate.dev`, piggyback on OSSF infrastructure, or ship no endpoint at all in v1 (local-only, no-op stub)? Recommendation: local-only in v1; revisit when we have > 1K users and a budget for infra. Impacts PR-6 scope.
- [ ] **Aikido Intel feed integration** — Include Aikido's AGPL-licensed malware feed (median detection 5 min vs OSSF 10 days) as an input to retroactive verification pipeline? Consuming is fine; redistributing triggers AGPL. Recommendation: consume as input only in PR-6, never bundle. Why it matters: materially improves retroactive FN detection lead time.
- [ ] **`--show-shadow` UX** — When `shadow.expose_preview=true` and user runs `aigate check <pkg> --show-shadow`, print concise two-line preview ("ML: SAFE(0.91, 28ms) | LLM: SAFE(0.95)") or full ShadowEvent dump? Lean concise; full dump available via `aigate debug ml-predict <pkg>`.
- [ ] **Isotonic vs sigmoid calibration** — Per external verification (sklearn issue #16321), isotonic introduces ties and can degrade ranking metrics; sklearn docs recommend sigmoid for small-n or very-imbalanced data. Training script should compute Brier on BOTH isotonic and sigmoid variants and select lower-Brier. Is this branch-and-pick acceptable, or do we want to pre-commit to one method for reproducibility?
- [ ] **Data-cutoff on npm training set** — OSSF npm label count dominated by Q3-Q4 2025 bulk ingests (142K in 2025-11 alone). Proposed: train on `published_at ≤ 2025-07-01`, hold Q4-2025 spike as adversarial test set. Alternative: drop npm entirely from v1 and ship PyPI-only ML. Impacts per-ecosystem generalization claims.

## ml-llm-hybrid-ipi-defense iteration 2 — 2026-04-22 (post-Architect REVISE-BEFORE-CRITIC)

- [x] **Aikido Intel feed inclusion** (iter-1 item #4 RESOLVED): consume-only via HTTPS, non-redistributed. Attribution documented in `docs/threat-intel-sources.md`. Aikido's own launch post explicitly states "developers may freely use, modify, and distribute." Apache-2.0 aigate wheel remains Apache-2.0. (2026-04-22 iter-2 planner)
- [x] **npm data-cutoff** (iter-1 item RESOLVED): two temporal test sets — `_stable` (pre-2025-07-01, promotion gate) and `_adversarial` (post-2025-07-01, diagnostic only). `scripts/promote_ml.py` reads `_stable` only. (2026-04-22 iter-2 planner)
- [x] **Telemetry endpoint ownership** (iter-1 item RESOLVED): `telemetry.endpoint: null` default; no endpoint shipped in v1; revisit post-MVP. (2026-04-22 iter-2 planner)
- [x] **ML artifact format** (NEW in iter-2, RESOLVED via Architect CRITICAL #1): `skops.io` primary (MIT, whitelist-deserialization, pickle-free); `joblib` only as heavily-gated fallback behind `allow_joblib_fallback: false` default. Pre-deserialization SHA256 verification as defense-in-depth. (2026-04-22)
- [x] **Dual-LLM default posture** (NEW in iter-2, RESOLVED via Architect HIGH #3): default-OFF; PR-3 metadata-only reader carries ~80% of protection at 50% cost; `aigate doctor --cost-projection` warns when enabling would regress >1.2× baseline. ADR alternatives explicitly document this. (2026-04-22)
- [x] **Short-circuit hard gates** (NEW in iter-2, RESOLVED via Architect HIGH #2): `min_pkg_age_days: 14`, `min_num_releases: 5`, `maintainer_set_changed_within_30d == false`, `require_any_of_repo_or_homepage_resolves: true`. All must pass regardless of ML confidence. (2026-04-22)
- [x] **Kill-switch multipliers** (NEW in iter-2, RESOLVED via Architect #8): 1.5× + 5 FN/24h floor for short-circuit; 2.0× + 10 FN/24h floor for general; unconditional floor on >1000 downloads/week package. (2026-04-22)
- [x] **Remote endpoints opt-in** (NEW in iter-2, RESOLVED via Architect #4): `kill_switch.remote_list_url` + `telemetry.endpoint` + `telemetry.remote_kill_switch_url` all default `null`. (2026-04-22)
- [x] **Metadata-field IPI scan** (NEW in iter-2, RESOLVED via Architect #9): `scan_metadata_fields: true` default; scans `PackageInfo.description` + `PackageInfo.author`. (2026-04-22)
- [x] **Schema-violation fallback** (NEW in iter-2, RESOLVED via Architect #10): `route_needs_human_review` (was `metadata_only_only`); rate-escalation at >5%/1000 calls. (2026-04-22)
- [x] **Context-aware short-circuit thresholds** (NEW in iter-2, RESOLVED via Architect #7): CI 0.90 / interactive 0.97. (2026-04-22)
- [x] **PR-5→PR-4 re-sequence** (NEW in iter-2, RESOLVED via Architect #11): ML shadow lands after PR-1 in parallel with Lane A; 4-week shadow clock starts earlier. (2026-04-22)
- [x] **pydantic as optional extra** (NEW in iter-2, RESOLVED via Architect #5): moved to `[project.optional-dependencies] ipi`; fallback to `json.loads` + manual validation when absent. (2026-04-22)
- [ ] **skops.io protocol version pinning** (NEW in iter-2): pin to single protocol per model version or allow range? Leaning single pinned protocol; bump triggers `MODEL_SHA256` update + two-person review. Why it matters: skops format is protocol-versioned and evolving; stability vs forward-compat tradeoff.
- [ ] **joblib fallback existence** (NEW in iter-2): ship `allow_joblib_fallback` at all, or remove the fallback entirely and go skops-only? Architect CRITICAL suggests "skops OR SHA256-verified-joblib"; iter-2 plan ships BOTH (skops primary, joblib heavily gated). Safer alternative: skops-only, fail-hard if skops cannot serialize. Why it matters: every code path that calls `pickle.load`/`joblib.load` on bytes is a latent RCE if a future refactor bypasses the SHA256 check. Decision needed before PR-4 merges.
- [ ] **Residual maintainer-takeover class for short-circuit** (NEW in iter-2, from Architect HIGH #2 residual): hard gates cannot catch maintainer-takeover on an established package >14 days old whose maintainer list has been stable for 30+ days. No static signal catches this. Is the mitigation (interactive defaults to 0.97 + mode defaults to shadow + CI operators accept tradeoff knowingly) sufficient for v0.6 short-circuit shipping, or should short-circuit ship as opt-in only (mode default `vote`, not `shadow`) until Stage-3? Decision affects v0.6 short-circuit shipping scope.
- [ ] **AGPL data-consumption legal interpretation for Aikido** (NEW in iter-2): plan's position aligns with Aikido's own launch-post statement and standard "HTTP data consumption ≠ derivative work" reading, but is not a formal legal opinion. Should aigate obtain explicit legal counsel before shipping PR-6 `retroactive.aikido_enabled: true` default, or is the documented-interpretation posture + `retroactive.agpl_notice_ack: true` config acknowledgment sufficient?

## ml-llm-hybrid-ipi-defense iteration 3 — 2026-04-22 (post-Architect iter-2 REVISE-BEFORE-CRITIC)

- [x] **skops CVE-2025-54412 pin** (BLOCKER-1 RESOLVED): pinned `skops>=0.12.0,<0.20` in `[ml]` extra; fix commit `0aeca055509dfb48c1506870aabdd9e247adf603`, advisory published 2025-07-25, CVSS 8.7. Added `tests/security/test_skops_cve_2025_54412.py` running POC against empty `skops_trusted_types: []` whitelist on every PR. (2026-04-22 iter-3 planner)
- [x] **`MODEL_SHA256`-inside-the-wheel self-contradiction** (iter-2 L83 fix): clarified that the constant is inside the Python module (shipped in wheel); what inside-wheel SHA256 buys (post-install tamper defense) vs does not buy (whole-wheel replacement). Whole-wheel integrity via PEP 740 Sigstore attestations at v0.6 release. (2026-04-22 iter-3 planner)
- [x] **Version-level cooldown hard gate** (BLOCKER-2 partial RESOLVED): `min_version_age_days: 7` added to short-circuit `hard_gates`; matches pnpm 10.16 / Yarn 4.10 / Bun 1.3 / npm 11.10.0 ecosystem norm shipped Sep 2025-Feb 2026. Applies to short-circuit SAFE only; ML backend still runs and still votes in LLM consensus if gate blocks short-circuit. (2026-04-22 iter-3 planner)
- [x] **Provenance-continuity hard gate** (BLOCKER-2 partial RESOLVED): `require_provenance_continuity: true` added; if any prior version had OIDC/PEP 740 attestation and current version drops it, gate blocks short-circuit. Matches pnpm 10.21 `trustPolicy: no-downgrade`. No-op when no prior version had attestation (~75-85% of packages). `PackageInfo.prior_provenance` + `PackageInfo.version_published_at` added to registry resolvers. (2026-04-22 iter-3 planner)
- [x] **consensus.py rebase gate** (non-blocking #3 RESOLVED): explicit rebase rule added — if two PRs touch `consensus.py._aggregate_votes` or shared helper, second PR MUST rebase on main AFTER first merges AND re-run full integration test suite. Codified in `.github/CODEOWNERS` + PR-template checklist + CI branch-protection. (2026-04-22 iter-3 planner)
- [x] **`agpl_notice_ack` default** (non-blocking #4 RESOLVED): flipped to `false`; user must explicitly set to `true` or pass `--agpl-ack` to use Aikido feed. On first Aikido fetch with `agpl_notice_ack: false`, aigate prints one-line note pointing to `docs/threat-intel-sources.md` and skips the Aikido join (OSV still runs). (2026-04-22 iter-3 planner)
- [x] **Model-size assertion** (non-blocking #5 RESOLVED): `tests/unit/test_model_size_budget.py` asserts `os.path.getsize("src/aigate/ml/models/metadata-ml-v1.skops") <= 1_048_576`; wired into CI on every PR and release tag. (2026-04-22 iter-3 planner)
- [x] **CI-mode 0.07-confidence-gap quantification** (non-blocking #6 RESOLVED): `docs/ml-short-circuit-threat-model.md` adds quantitative section — ~2-5% of short-circuit-eligible packages in `[0.90, 0.97)` band (literature-derived prior from Brier ≤0.05 calibration distribution). CI operators can opt into `confidence_threshold_ci: 0.97` to close the gap at ~15-25% short-circuit-rate cost. Re-measure at Stage-1 promotion. (2026-04-22 iter-3 planner)
- [ ] **Class-D-Tail residual (iter-3 honest-residual)** — compromised version that sits >7 days AND (no prior provenance OR attacker controls publishing identity) passes all iter-3 hard gates. Mitigated (not prevented) by daily OSV+Aikido retroactive join + kill-switch unconditional floor. Full resolution requires sigstore attestation-transparency-log verification (ADR follow-up) or behavioral-at-install sandboxing (out of aigate scope). Is this residual acceptable for v0.6 short-circuit promotion (Stage-1) or should Stage-1 require an additional evidence gate (e.g. "no confirmed Class-D-Tail FN in the last 8 weeks of shadow data")? Leaning: add Class-D-Tail monitoring to Stage-1 promotion criteria in PR-7; block promotion if any Class-D-Tail FN observed in shadow window.
- [ ] **`PackageInfo.prior_provenance` schema stability** (iter-3 NEW) — iter-3 adds `prior_provenance: dict | None` to `PackageInfo`. Shape: `{"npm": {"5.2.6": {"has_attestation": true, "publisher_identity": "npm-user:chalk-bot"}, ...}, "pypi": {"0.6.1": {"has_pep740_attestation": false}, ...}}`. Is this the right normalized shape, or should we split into `prior_provenance_npm` / `prior_provenance_pypi`? Leans unified dict for future-ecosystem extensibility; revisit if a third ecosystem arrives.
- [ ] **Stage-1 promotion criteria addition for Class-D-Tail** (iter-3 NEW) — PR-7 promotion gate should include "zero confirmed Class-D-Tail FN in the last 4 weeks of shadow data (retroactive OSV+Aikido join)." Concretely: if retroactive verification finds ANY confirmed-malicious package that was short-circuited AND (version_age_days ≥ 7 AT SCAN TIME) AND (no prior provenance OR provenance-continuity held) → block Stage-1 promotion. Open: is 4 weeks the right window, or should it be 8 weeks (matching full shadow-mode evidence gate)? Leans match the full window (8 weeks = extended shadow clock).
